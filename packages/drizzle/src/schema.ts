import { conversations, conversationsRelations } from "./schema/conversations";
import { scenarios } from "./schema/scenarios";
import {dictionaryElements, dictionaryElementsEnum} from "./schema/dictionary-elements";

export const schema = {
    conversations,
    conversationsRelations,
    scenarios,
    dictionaryElements,
    dictionaryElementsEnum
};